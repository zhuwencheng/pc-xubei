/**
 * Created by Administrator on 2017/5/27.
 */
define(['jquery','text!component-site/transfer-dialog.html','css!component-site/css-site/transfer-dialog.css',''],function($,tem){
   /* layer.config({
        path: '../js-site/layer/' //layer.js所在的目录，可以是绝对目录，也可以是相对目录
    });
    return function(callback){
        return layer.ready(function(){
            layer.open({
                type: 1,
                title:'购买',
                area: ['760px', '550px'],
                content: tem //这里content是一个普通的String
            });
            callback&&callback();
        })
    };*/

    return tem;


});